# AGENTS Instructions: server-tools

This repository is part of the Phase 5 Wave 1 B3 rollout.

Repo rules:
- Keep the tracked tree limited to reusable server automation and generic examples.
- Do not commit host inventory, FQDNs, usernames, secrets, certificates, or site-specific orchestration bindings.
- If you add tracked defaults or setup notes, update `README.md` and `scripts/sanitize_check.sh` in the same change.
- Run `bash scripts/sanitize_check.sh` before handoff.